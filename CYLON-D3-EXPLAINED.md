# Cylon D3 战役报告（白话版）

> 2026-09-07 · 分支 cylon-v9.1-type2 · 未提交
> 术语版：exp/e1c_paper/ANALYSIS.md §5.3/5.4；操作纪律：CYLON-USAGE.md §8.10.4 / §8.11
> 本文 = 面向人的完整叙事，读完应能回答：这轮干了啥、发现了啥、还剩啥。

## 0. 谁在跟谁说话

我们的模拟器里有一个"智能 SSD"——设备里除了存数据，还塞了一个计算引擎（想象 SSD 里焊了一块能干活的芯片）。虚拟机里跑着搜索程序（在 2100 万条向量里找最近邻），它把一部分活儿外包给设备引擎，剩下的自己算。

两边沟通靠一个**信箱**：共享内存里的一小块。客户端把"任务卡"放进信箱，然后等；引擎一直盯着信箱，看到活儿就拿走、干完、把结果放回、插一面"完成"小旗。客户端看到旗，取走结果，再投下一单。

## 1. D3 改了两样东西

**信箱协议升级（v2）。** 原来信箱用一面 32 位的小旗传状态，客户端和引擎**两边都写这面旗**。并发编程里"多个写者抢一个字"是经典雷区。v2 改成一个 64 位字：低半是状态，高半是**流水号**（每投一单 +1）。规矩变成：流水号只有客户端能动；引擎靠核对流水号确认"这是新任务，不是上一单的残影"。歧义源被拆掉了。

**加了个门铃。** 原来客户端等结果是**死等**：一个循环高速盯信箱（轮询）。又烧 CPU，又意味着客户端那个 vCPU 一直在全速跑——我们怀疑这种全速自旋本身参与了制造崩溃。门铃是一块我们新做的模拟 PCI 设备：引擎干完活就向虚拟机发一个硬件中断，客户端可以睡死过去，被中断叫醒再去取结果。

## 2. E-M 实验：换完之后还崩吗

在两个毒格上，各用三种"等方式"各跑 3 遍：

| 等方式 | 白话 |
|---|---|
| poll = 死等 | 和崩溃发生时一模一样的盯法 |
| sleep = 打盹等 | 盯 2 微秒，眯 50 微秒，再看 |
| doorbell = 等门铃 | 睡死，等中断叫醒再去拿结果 |

每遍都查一件事：**结果文件是否和基准逐字节相同**。这是硬门禁——不管怎么改，算出来的结果必须一个字节都不变，否则改动本身就是错的。

结果：

- **标量毒格 9/9 全活**（三种方式各 3/3）。同一配置历史上是 3/3 必死 → **治好了**。
- **AVX 毒格 10/10 全死**（三种方式 9 次 + 一次额外尝试）→ **没治好**。

## 3. 治好的功劳算谁的？（本轮最值钱的结论）

新世界有两处代码变了：客户端变了，引擎也变了。到底是哪边治好的？

**对照实验（CONTROL）**：拿**老客户端**（协议和等法和历史崩溃时一模一样、一行没改）对**新引擎**跑同一毒格。结果：**活着**，146.265s，结果逐字节正确。

结论很干净：客户端一行没改也能活 → **治愈代码在引擎侧**。v2 引擎用一次 64 位写入同时发布"完成+流水号"（不存在中间态），并且靠流水号拒收残影任务；v1 引擎是"两个写者抢一面旗"。

附带收获：这个毒格正是之前实验矩阵里唯一因崩溃而空缺的格子，现在补上了，矩阵完整。

## 4. AVX 那类为什么治不好（D3-F 已破案，见 §9）

它有个鲜明特征：程序启动后 **约 1.3 秒**就死——搜索根本还没开始。当时推断：根源
大概率是引擎在搜索期间的高频**内存页模式切换**竞态。这个推断**只对了一半**：崩溃
确实需要"引擎翻页"暴露，但直接死因是——

**模拟器宿主的内核（KVM）里有个老旧的 x86 指令编译器——不，是模拟器，它不认识
现代 AVX 每条指令的"写法"。**客户端第一单任务卡的写入用的是 AVX 写法；如果信箱
恰好处于"收费岗"模式（上个 run 收尾时的竞态留下了这个状态），本该"收费+放行"的
老路径要先"读懂"这条指令才能处理，模拟器读不懂 AVX → 直接给客户端发了一个"非法
指令"信号把程序杀了。标量（老式指令）客户端读得懂 → 永不死，还顺手把收费岗还原
成直通（"自愈"）。三等待臂全灭 + 启动后 1.3s 死 = 这一个机理全部解释。

## 5. 坑：老客户端必须第一个用信箱

对照实验第一次跑就失败：老客户端投的任务石沉大海。查明原因：

老客户端只写信箱的低 32 位（它不认识高半字）。如果这个信箱槽位**之前被新客户端用过**，高半字里留着上一个会话的旧流水号。新引擎一看：低半字是"有活"，高半字流水号不是它期待的下一个号 → 判定"过期任务"，拒收。老客户端不知道自己被拒，干等到超时。

实际影响只有一条操作纪律：**老客户端必须在模拟器刚重启后的第一时间使用**（那时信箱是干净的）。已写进文档。

## 6. 门禁：确认改动没破坏任何原有东西

四项全过——老协议全流程 112.485s ✓、门铃全流程 114.239s（中断真实发生 515 次）✓、新协议标准点 112.869s（落在正常波动带）✓、对照实验 146.265s ✓，结果文件全部逐字节正确。

一个有价值的副产物：**门铃的耗时和死等完全一样**（单任务约 200ms，中断唤醒只花微秒级，测不出来）。门铃的价值不在提速，在于客户端不必烧着一个 CPU 核干等。

## 7. 交付物（都在仓库，按约定未提交）

- 模拟器代码：v2 协议 + 门铃设备
- 数据：exp/e1c_paper/em_results.csv（21 行）+ em_battery.png（一张图）
- 文档：ANALYSIS.md §5.3、CYLON-USAGE.md §8.10.4 与 §8.11、CYLON-TYPE2.md §3/§5/§7
- 一键脚本 em_p2_bringup.sh（每次开机一步备齐实验环境；以前是散落多步的手工活，固化成脚本）
- 取证与修复：core dump 工具链修复（apport 吞 core 坑）+ KVM exit 40 补丁（内核 3 文件 + QEMU 3 文件）+ rev -13 内核 deb + 重建的 QEMU

## 8. 等你拍板的三件事

1. **commit**：D1–D3 的改动全堆在工作区，等你明示再提交。
2. **门铃投递"间歇死"已破案 + 电池收口（2026-09-08 凌晨）**：真因 = 门铃内核模块
   二进制陈旧——BusMaster 修复写进了源码（9/6 18:54）但**从未重编**（旧 .ko 是 18:17
   的），历次 boot 一直加载无修复的旧模块 → MSI 写被静默丢弃。重编后投递复活，且
   **电池门铃臂 ×3 复跑 3/3 活 byte-identical（165.5/163.7/162.0s，与 poll/sleep
   同带）+ G5 门禁 117.1s 复活**（替换 9/7 死节奏假数据 5004.5s）。E-M 电池 21 行
   全 PASS，门铃时序无害复确认。**未决**：scalar 格 fixed-binary 复跑（低优先级，
   9/6 数据已是 live 投递）、移除调试打印。
3. **D4**：路线图最后一项（给设备加标准化的"自我介绍"描述表，纯外观件，零性能影响），按约定没动，等你指令。

## 9. D3-F 取证与正面修复（2026-09-07 已实现，已装机，F6 验收通过）

- **取证**（正面回答"翻页竞态"旧论）：给客户端开 core dump（先改掉 apport 管道
  吞 core 的坑），毒点复现 ×2 死于同一偏移 0x2983（mb_submit+131 的 `vmovq` 存储，
  rax = 信箱页）；scalar 同点活且自愈；avx 后行活且 dump 逐字节正确（158.9s 带内）。
  内核证据链：DUAL 槽缺页 → 内核装"陷阱叶"→ 进老模拟器 → VEX 解码失败 → #UD。
- **修复（F5 "计费+重执行"）**：内核对 Cylon 收费页的访问不再进那个老模拟器，改
  为把"谁在访问（地址+读/写）"递给 FEMU（新 KVM 退出码 KVM_EXIT_CYLON_DER = 40）；
  FEMU 走与老路径完全相同的计费（信箱页 = 插销+BI 计费；数据页 = FTL 计费+翻直），
  放行后指令**原生重执行**——不读指令内容，AVX/标量一视同仁。**计费逐项等价、
  真实翻页机制原封不动**（红线保住）。同 gpa 重试 3 次失败自动回退老路径（兜底）。
- **F6 验收（2026-09-07 晚）**：门禁 G1–G4 全过（**G4 = 前 GPF 崩溃格 b250_f050
  avx 一次过**）；电池 avx 毒格 poll/sleep ×3 = **6/6 活**，dump 全 byte-identical
  （poll 165.4/161.6/160.9s，sleep 161.8/161.9/161.8s）——avx #UD 族治愈成立。
  另破 QEMU GPF 案（cache UAF 竞态，cache_lookup 无锁 g_tree_lookup 撞 evict+free，
  已修：lookup 加锁 + insert 锁内 lpn 复查）。**遗留已清（2026-09-07 深夜破案）**：
  门铃 MSI 投递"间歇死"的真因 = **门铃内核模块二进制陈旧**：BusMaster 修复写进源码
  （9/6 18:54）但从未重编（.ko 一直是 18:17 的旧构建）→ 历次 boot 加载的模块没有
  `pci_set_master` → QEMU 把设备 MSI 写空间门控在 bus master 位上 → MSI 写落
  unassigned 空间**静默消失**（零报错）。重编 .ko → BusMaster+ → SIGNAL_MSI ret=1
  → 投递复活。**教训**：改 .ko 源码后必须重编并 `nm -u` 核对符号；dump 门禁对投递死
  不敏感，wall<180s=活 / >5000s=死 是唯一判别器。
  **电池收口（9/8 凌晨）**：门铃臂 ×3 复跑全过（165.5/163.7/162.0s byte-identical，
  与 poll/sleep 同带）+ G5 117.1s 复活 → E-M 电池 21 行全 PASS。
